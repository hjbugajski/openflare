# ==============================================================================
# OpenFlare Configuration
# ==============================================================================
#
# MINIMAL PRODUCTION SETUP (4 required + 3 for email):
#
#   APP_KEY=base64:...
#   APP_URL=https://your-domain.com
#   REVERB_APP_KEY=<random>
#   REVERB_APP_SECRET=<random>
#
#   # For email notifications:
#   MAIL_MAILER=resend
#   MAIL_FROM_ADDRESS=noreply@your-domain.com
#   RESEND_API_KEY=re_...
#
# ==============================================================================

# ------------------------------------------------------------------------------
# REQUIRED - Application
# ------------------------------------------------------------------------------

APP_KEY=
# Generate with: php artisan key:generate --show

APP_URL=http://localhost:8000
# Your full application URL including protocol.
# In production: https://your-domain.com

# ------------------------------------------------------------------------------
# REQUIRED - WebSockets (Reverb)
# ------------------------------------------------------------------------------

REVERB_APP_KEY=openflare-key
REVERB_APP_SECRET=openflare-secret
# Generate secure values for production:
#   openssl rand -hex 16  (for REVERB_APP_KEY)
#   openssl rand -hex 32  (for REVERB_APP_SECRET)
#
# All other Reverb settings (host, port, scheme) are auto-derived from APP_URL.

# ------------------------------------------------------------------------------
# EMAIL (optional - notifications logged by default)
# ------------------------------------------------------------------------------

MAIL_MAILER=log
# Options: log (default, emails logged), resend (production)

MAIL_FROM_ADDRESS=hello@example.com
# Sender address for all emails

RESEND_API_KEY=
# Required when MAIL_MAILER=resend

# ------------------------------------------------------------------------------
# OPTIONAL - Database (defaults work for most cases)
# ------------------------------------------------------------------------------

# DB_CONNECTION=sqlite
# Default: sqlite. Use 'pgsql' for PostgreSQL.

# DB_DATABASE=
# SQLite: defaults to database/database.sqlite
# For Railway volume: /data/database.sqlite

# DATABASE_URL=
# PostgreSQL connection URL (alternative to individual DB_* vars)
# Railway provides this automatically when you add Postgres

# ------------------------------------------------------------------------------
# OPTIONAL - Development Overrides
# ------------------------------------------------------------------------------

# APP_ENV=local
# Default: production

# APP_DEBUG=true
# Default: false

# ------------------------------------------------------------------------------
# OPTIONAL - Monitors (all have sensible defaults)
# ------------------------------------------------------------------------------

# MONITORS_TEST_MODE=false
# Disable actual HTTP checks (for testing UI)

# MONITORS_DISPATCH_LIMIT=500
# Max checks per scheduler run

# MONITORS_RETENTION_DAYS=30
# Days to keep check history

# MONITORS_MAX_PER_USER=100
# Max monitors per user

# ==============================================================================
# DEPLOYMENT QUICK START
# ==============================================================================
#
# LOCAL DEVELOPMENT:
#   1. Copy this file to .env
#   2. Run: php artisan key:generate
#   3. Run: composer run dev
#
# RAILWAY (Docker):
#   Set these environment variables in Railway dashboard:
#
#   APP_KEY                 → php artisan key:generate --show
#   APP_URL                 → https://your-app.railway.app
#   REVERB_APP_KEY          → openssl rand -hex 16
#   REVERB_APP_SECRET       → openssl rand -hex 32
#   MAIL_MAILER             → resend
#   MAIL_FROM_ADDRESS       → noreply@your-domain.com
#   RESEND_API_KEY          → your Resend API key
#
#   Optional (for persistent SQLite):
#   DB_DATABASE             → /data/database.sqlite
#   + Attach a Railway volume mounted at /data
#
#   Or use Railway Postgres:
#   DB_CONNECTION           → pgsql
#   DATABASE_URL            → ${{Postgres.DATABASE_URL}}
#
# ==============================================================================
